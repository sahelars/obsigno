{
	"name": "obsigno",
	"version": "0.1.3",
	"description": "Use obsigno to easily sign, verify, and create custom messages",
	"license": "MIT",
	"author": "Sam Larsen",
	"main": "index.js",
	"bin": {
		"obsigno": "./bin/cli.js"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/sahelars/obsigno"
	},
	"scripts": {
		"test": "mocha",
		"lint": "prettier --use-tabs --trailing-comma 'none' --check .",
		"format": "prettier --use-tabs --trailing-comma 'none' --write ."
	},
	"devDependencies": {
		"chai": "^4.5.0",
		"mocha": "^10.7.0",
		"prettier": "^3.2.5"
	}
}
